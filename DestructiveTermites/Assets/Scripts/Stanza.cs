using UnityEngine;
using UnityEditor;
using System.Collections;

public class Stanza{

	// Use this for initialization
	void Start () {
       /*  Random r = new Random();
       int n = Random.Range(Costanti.NUMERO_DI_COPIE_MIN, Costanti.NUMERO_DI_COPIE_MAX);
        for (int i = 0; i < n; i++)
        {
            //GameObject prefab = Instantiate(Resources.Load("Player", typeof(GameObject))) as GameObject;
            //prefab.layer = LayerMask.NameToLayer("Persone");
            //GameObject clone = Instantiate(prefab, Vector3.zero, Quaternion.identity) as GameObject;
            //clone.transform.position = Vector3.one;
        }*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
